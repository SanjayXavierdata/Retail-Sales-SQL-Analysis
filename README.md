# 🛍️ Retail Sales Analysis using SQL

## 📌 Project Overview

This project focuses on analyzing retail sales data using **SQL** to extract meaningful business insights. The analysis covers customer demographics, sales performance, product categories, purchasing patterns, and time-based sales trends.

The goal is to demonstrate practical SQL skills by transforming raw retail transaction data into useful insights that can support business decision-making.

---

## 🎯 Project Objectives

* Analyze overall retail sales performance
* Identify top-performing product categories
* Understand customer purchasing behavior
* Analyze sales by gender and age
* Identify sales trends based on date and time
* Find high-value transactions
* Analyze hourly and shift-based sales patterns
* Practice SQL aggregation, filtering, grouping, and date/time functions

---

## 🗂️ Dataset

The dataset contains retail transaction-level information.

### Key Columns

| Column            | Description                  |
| ----------------- | ---------------------------- |
| `transactions_id` | Unique transaction ID        |
| `sale_date`       | Date of the sale             |
| `sale_time`       | Time of the sale             |
| `customer_id`     | Unique customer ID           |
| `gender`          | Customer gender              |
| `age`             | Customer age                 |
| `category`        | Product category             |
| `quantity`        | Number of products purchased |
| `price_per_unit`  | Price per unit               |
| `cogs`            | Cost of goods sold           |
| `total_sale`      | Total transaction value      |

---

## 🛠️ Tools & Technologies

* **MySQL**
* SQL
* GitHub
* Excel / CSV Dataset

---

## 🔍 SQL Analysis Performed

The project includes SQL queries to answer business-related questions such as:

1. What is the total number of sales transactions?
2. What is the total revenue generated?
3. Which product categories generate the highest sales?
4. What is the average age of customers?
5. How many transactions were made by each gender?
6. What are the sales generated by different customer genders?
7. Which dates had the highest number of sales?
8. What are the top-selling product categories?
9. What are the highest-value transactions?
10. Which shift — Morning, Afternoon, or Evening — has the highest number of orders?

---

## 📊 Key SQL Concepts Used

The project demonstrates the use of:

* `SELECT`
* `WHERE`
* `GROUP BY`
* `ORDER BY`
* `HAVING`
* `COUNT()`
* `SUM()`
* `AVG()`
* `MAX()`
* `MIN()`
* `CASE WHEN`
* `EXTRACT()` / date functions
* Time functions
* Aggregate functions
* Subqueries
* Common Table Expressions (CTEs)

---

## 📈 Example SQL Query

### Sales by Category and Gender

```sql
SELECT 
    category,
    gender,
    COUNT(*) AS transaction_count
FROM retail_sales
GROUP BY category, gender
ORDER BY category;
```

This query groups transactions by **product category and customer gender** to understand purchasing patterns.

---

## ⏰ Sales by Shift

Sales can also be categorized into different time periods:

```sql
WITH hourly_sales AS (
    SELECT *,
        CASE
            WHEN HOUR(sale_time) < 12 THEN 'Morning'
            WHEN HOUR(sale_time) BETWEEN 12 AND 17 THEN 'Afternoon'
            ELSE 'Evening'
        END AS shift
    FROM retail_sales
)
SELECT 
    shift,
    COUNT(*) AS total_orders
FROM hourly_sales
GROUP BY shift
ORDER BY total_orders DESC;
```

This helps identify the time of day when the store receives the highest number of orders.

---

## 💡 Business Insights

The analysis can help businesses understand:

* Which product categories perform best
* Which customer segments generate more transactions
* Peak purchasing periods
* Customer purchasing behavior
* Revenue-generating categories
* High-value transactions
* Opportunities to optimize sales during different time periods

---

## 📁 Project Structure

```text
Retail-Sales-SQL-Project/
│
├── retail_sales.csv
├── retail_sales_analysis.sql
├── README.md
└── screenshots/
    └── sql_results.png
```

## 📌 Skills Demonstrated

**SQL | Data Analysis | Data Cleaning | Data Aggregation | Business Analytics | MySQL | Data Visualization**


